import connectMongo from "@/util/connectMongo";
import Booking from "../../../../../../../models/booking";

export default async function createPayment(req: any, res: any) {
  try {

  } catch (e) {
    console.log("Database error: ", e);
  }
}
